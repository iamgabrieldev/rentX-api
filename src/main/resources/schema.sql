CREATE TABLE CAR (
                         ID INTEGER PRIMARY KEY AUTO_INCREMENT,
                         NAME VARCHAR(255),
                         DESCRIPTION VARCHAR(255),
                         DAILY_RATE NUMERIC(20,2),
                         LINCENSE_PLATE VARCHAR(10),
                         FINE_AMOUNT NUMERIC(20,2),
                         BRAND VARCHAR(50)
);
